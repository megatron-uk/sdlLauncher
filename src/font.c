#include <SDL/SDL.h>
#include "font.h"

/*
	Turn a string of text into a SDL surface represented
	by one of more bitmap fragments from the font bitmap.
*/
SDL_Surface* text2surface(char *text){

	SDL_Surface *text_surface = NULL;
	return text_surface;
}